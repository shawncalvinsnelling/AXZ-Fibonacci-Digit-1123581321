# GitHub Publishing

1. Create repository `AXZ-Fibonacci-Digit-1123581321`.
2. Upload all files, including `.github/workflows/verify.yml` and `.gitignore`.
3. Confirm Actions runs `Verify finite certificate`.
4. Publish release `v1.0.0` after Actions succeeds.
