[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "axz-fibonacci-digit-1123581321"
version = "1.0.0"
description = "Finite exact certificate for the Fibonacci digit no-division ordered arithmetic gap."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}

[tool.setuptools.packages.find]
where = ["src"]
